export const TILE_SIZE = 5;
export const GRID_SIZE = 20;
export const AREA_SIZE = GRID_SIZE / 2;
export const MAP_SIZE = GRID_SIZE * TILE_SIZE;
export const OBSTACLE_DENSITY = -0.3; // Value between -1 and 1
